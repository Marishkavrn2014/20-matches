//
package main.java.shadrina.marina.epam.service;

public interface ArtificialIntelligence {

    // the method should select the number of matches based on
    // the current number of matches on the table, which is transmitted as an argument
    int chooseMove(int currentMatches);
}
